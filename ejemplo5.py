# Escribir los siguientes comandos en la consola de Scapy:

# >>> cap=sniff(iface='mon0',count=3)
# >>> cap
# >>> cap.show()
# >>> cap[0].show()
# >>> cap[0].pdfdump('/tmp/test.pdf')
# >>> cap=sniff(iface='mon0',count=800)
# >>> cap
# >>> cap.summary()

print "Lea el fichero ejemplo5.py para ver su contenido"
